Анализ отзывов сервиса Самокат

Собрал и проанализировал ~10 000 отзывов о сервисе доставки продуктов "Самокат" с сайта Т-Банка — что клиентам нравится, на что жалуются, и как меняется отношение по времени.

Что делает
Сбор данных — забирает отзывы через API сайта (нашёл эндпоинт через DevTools, вместо того чтобы кликать по кнопке "Показать ещё" в браузере — так в разы быстрее: ~10 000 отзывов за 2 минуты вместо часа).
Очистка текста — убирает мусорные символы, приводит "ё"/"ë" к одному виду, лемматизирует слова (через pymorphy3), отдельно обрабатывает частицу "не" (чтобы "не нравится" не разваливалось на два нейтральных слова).
Анализ — считает, какие фразы чаще всего встречаются в плохих и хороших отзывах (TF-IDF + логистическая регрессия), группирует отзывы по темам (доставка, цены, качество продуктов, курьеры, приложение), строит графики: распределение оценок, динамика доли негативных отзывов по месяцам, средний рейтинг по темам.
(опционально) LLM-саммари — прогоняет выборку отзывов через Gemini API, чтобы получить текстовое резюме в духе "что чинить в первую очередь". Тут стоит быть осторожным: модель иногда добавляет цифры и детали, которых не было в исходных данных — использовать как черновик для формулировок, а не как источник фактов.
Стек

Python, pandas, requests, pymorphy3, scikit-learn (TF-IDF, LogisticRegression), matplotlib, wordcloud. Для LLM-части — Google Gemini API.

Структура
data/raw/reviews_raw.xlsx        — сырые данные с парсера
data/processed/reviews_final.xlsx — очищенные и лемматизированные отзывы
analysis.ipynb                    — весь код анализа
eda_report.png                    — графики (распределение оценок, динамика негатива)
aspect_ratings.png                — рейтинг по темам
wordclouds.png                    — облака слов
wordclouds_clean.png              — облака слов
Как запустить
bash
git clone <repo>
cd <repo>
pip install -r requirements.txt
jupyter notebook analysis.ipynb

Если хотите использовать LLM-саммари — создайте .env файл с GOOGLE_API_KEY=....